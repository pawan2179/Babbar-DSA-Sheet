#include<bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin >> n;

	int k;
	cin >> k;

	int arr[k];
	for (int i = 0; i < k; i++) {
		cin >> arr[i];
	}

	vector<int> vec;

	int sum = 0;
	int max_sum = 0;

	for (int i = 0; i < k; i += (k / n)) {

	}

	for (auto x : vec)    cout << x << " ";

	return 0;

}